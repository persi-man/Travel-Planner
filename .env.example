# Database Configuration (legacy — app now uses IndexedDB in the browser)
# DATABASE_URL is only needed if running the old Docker/SQLite setup
# DATABASE_URL=file:./dev.db

# GitHub Pages static export
# GITHUB_PAGES=true

# Node Environment (development | production | test)
NODE_ENV=development
